import {NotificationReducerState,NotificationAction} from '.';

type FilterPropType = {
    state:NotificationReducerState,
    dispatch: React.Dispatch<NotificationAction> 
}

const Filter = (props:FilterPropType)=>{
    return <div>
        
    </div>
}
export default Filter;